name: diabetes-prediction-ci

# Definisikan lingkungan conda yang akan digunakan
conda_env: conda.yaml

# Definisikan perintah yang dapat dijalankan (Entry Points)
entry_points:
  # Ini adalah entry point default yang akan dipanggil untuk training
  main:
    command: "python modelling.py"
